export interface IPortForWard {
  daemonserverpid: number;
  port: string;
  reason: string;
  role: string;
  servicetype: string;
  status: string;
  sudo: boolean;
  svcName: string;
  updated: string;
}
